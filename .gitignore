# ── secrets — never commit ──────────────────────────────
.env
.env.*
*.env
!*.env.example
.dev.vars

# per-user editor/tool config (can contain tokens)
.vscode/
mcp.json
**/.claude/*
!/.claude/skills/
/.claude/skills/*/data/
.vs/

# ── dependency + build caches ───────────────────────────
node_modules/
dist/
.wrangler/
.tmp/
*.log

# ── local machine junk ──────────────────────────────────
*conflicted copy*
.git.disabled/

# ── nested repos that live on their own remotes ─────────
Workbook/covenant-reader/
MCP/

# Claude auto memory, machine-local
.claude/memory/
